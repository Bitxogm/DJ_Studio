pnpm exec lint-staged

echo "🔒 Escaneando secretos con gitleaks..."
docker run --rm -v "$(pwd):/repo" -w /repo zricethezav/gitleaks:latest \
  protect --staged --redact -v --config /repo/.gitleaks.toml
